import { CourseHighlightingDirective } from './course-highlighting.directive';

describe('CourseHighlightingDirective', () => {
  it('should create an instance', () => {
    const directive = new CourseHighlightingDirective();
    expect(directive).toBeTruthy();
  });
});
